services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel run
    networks:
      - tunnel-net
      - wwtp-net

networks:
  tunnel-net:
    external: true
  wwtp-net:
    external: true
